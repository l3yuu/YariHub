export type SectionPageProps = {
  /** Extra top padding when the section is the first block on a dedicated route */
  standalone?: boolean;
  /** Landing-page preview: show link to the full page */
  preview?: boolean;
  moreHref?: string;
};
